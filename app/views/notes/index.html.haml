.container.center
	%p.header Your Notes.
	-unless @notes.blank?
		.row
			- @notes.each do |note|
				%a{ href: (url_for [note])}
					.card.listcard.pink.darken-1.col.s4
						.title
							%p.white-text= note.title
						.updated-time
							%p.badge.white-text.right= "updated " + time_ago_in_words(note.updated_at) + " ago"	
		.row
			.center	
				%a= link_to "New Note", new_note_path, class: "waves-effect btn-raised btn-large"

	- else
		%p You haven't created any notes yet!
		%a= link_to "New Note", new_note_path, class: "waves-effect btn-raised btn-large"					
